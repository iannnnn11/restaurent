import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-booktable',
  imports: [RouterLink],
  templateUrl: './booktable.html',
  styleUrl: './booktable.css',
})
export class Booktable {}
